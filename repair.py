import sys

content = """# 컴퓨터 구조 심화 (computer2) 강의계획서

## 1. 교과목 개요

- **과목 위치**:
  ```
  basic  (컴퓨터 기초)
    ↓
  code   (개발 기초·도구)
    ↓
  computer  (컴퓨터 구조 기초 — 하드웨어 이해) ← 선수 이수 필수
    ↓
  computer2 (컴퓨터 구조 심화 — 하드웨어를 코드로 제어) ← 이 과목
    ↓
  os    (운영체제)
  ```
- **과목 소개**: `computer`에서 하드웨어를 *이해*했다면, 이 과목에서는 **그 하드웨어를 코드 수준에서 제어하고 소프트웨어 성능을 끌어올리는 심화 역량**을 기릅니다. 심하게 어려운 시스템 프로그래밍의 늪에 빠지기보다는, **기초부터 심화까지 이어지는 전반적 이해**를 지향합니다. 컴퓨터 구조가 컴파일러, 캐시 최적화, GPU 가속(CUDA/JAX/MLX), 병렬 및 클러스터링 기술, 하드웨어 가상화와 어떻게 맞닿아 있는지 실습 중심으로 자연스럽게 학습합니다.
- **핵심 질문**: "내 코드는 싱글코어를 넘어 멀티코어와 클러스터에서 어떻게 병렬로 실행되며, 플랫폼과 추상화 계층(가상화/런타임) 위에서 성능을 잃지 않는가?"

---

## 2. 학습 성과(LO)

- 컴파일러가 C/C++ 코드를 어셈블리로 변환하는 과정을 단계별로 추적하고, 최적화 플래그별 출력 차이를 분석할 수 있다.
- CPU 파이프라인·분기 예측·비순서 실행이 코드 성능에 미치는 영향을 `perf stat`으로 측정·설명할 수 있다.
- 캐시 계층 구조를 이해하고, 캐시 친화적 데이터 구조와 접근 패턴으로 실제 코드 성능을 향상시킬 수 있다.
- 싱글 프로세싱과 병렬 프로세싱의 한계를 이해하고 클러스터링의 확장을 학습한다.
- AVX2/NEON SIMD 인트린식으로 루프를 벡터화하여 스칼라 코드 대비 성능을 향상시킬 수 있다.
- 하드웨어 가상화와 언어 런타임의 추상화 비용을 체감할 수 있다.
- Flame Graph·`perf`·Cachegrind·Roofline 모델로 프로그램 병목을 체계적으로 진단할 수 있다.
- FreeRTOS 기반 임베디드 소프트웨어에서 태스크·큐·세마포어를 활용하여 멀티태스크 펌웨어를 작성할 수 있다.

---

## 3. 주요 실습 환경

| 도구 | 용도 |
|------|------|
| **godbolt.org (Compiler Explorer)** | C/C++ 코드를 다양한 컴파일러·최적화 플래그로 실시간 어셈블리 변환 |
| **GCC/Clang + objdump** | 실제 바이너리 역어셈블, 최적화 패스 리포트 확인 |
| **perf (Linux)** | CPU 성능 카운터 기반 프로파일링, Flame Graph 생성 |
| **Cachegrind / Valgrind** | 캐시 미스 수 측정, 메모리 접근 패턴 분석 |
| **CUDA / JAX / MLX** | GPU 커널 작성·프로파일링, 멀티 플랫폼 병렬 처리 효율성 체감 |
| **STM32CubeIDE / Arduino IDE** | MCU 임베디드 소프트웨어 개발 및 UART 디버깅 |
| **Python + 멀티프로세싱** | 병렬 처리와 분산 방식의 성능 측정 및 비교 확인 |

---

## 4. 14주 주차별 강의계획

---

### 1주차
- **대주제**: CPU 마이크로아키텍처를 소프트웨어 관점으로 — 내 코드는 어떻게 실행되는가
- **세부학습목표**: 슈퍼스칼라·비순서 실행·투기적 실행이 코드 성능에 어떤 영향을 주는지 이해하고, `perf stat`으로 CPU 하드웨어 카운터를 직접 측정해 본다.

#### 📌 1-1. 현대 CPU 파이프라인 심화
1. 슈퍼스칼라(Superscalar) — 같은 사이클에 여러 명령어를 동시에 디코드·발행하는 멀티발행 구조
2. 비순서 실행(Out-of-Order Execution, OoOE) — 의존성 없는 명령어를 먼저 실행하여 파이프라인 스톨 감소
3. 투기적 실행(Speculative Execution) — 분기 결과 이전에 예측 경로를 미리 실행, 틀리면 롤백
4. 재순서 버퍼(ROB, Reorder Buffer) — OoOE 실행 결과를 프로그램 원래 순서로 커밋하는 하드웨어
5. Spectre/Meltdown — 투기적 실행이 만든 보안 취약점, 소프트웨어 패치(Retpoline)가 성능에 미치는 비용

#### 📌 1-2. 분기 예측과 코드 패턴
1. 1비트·2비트 포화 카운터 — 과거 이력으로 분기 방향(Taken/Not-Taken)을 예측하는 하드웨어
2. 분기 목적지 버퍼(BTB) — 이전에 실행한 분기의 목적지 주소를 캐시하여 IF 단계에서 즉시 예측
3. 오예측 패널티 — 현대 CPU에서 분기 오예측 시 15~20사이클 낭비, 반복 루프에서의 영향
4. 코드로 예측률 높이기 — `__builtin_expect(x, likely)`, 정렬된 데이터에서 `if`를 쓰는 패턴
5. 조건 없는 코드(Branchless) — 분기 대신 산술·비트 연산으로 조건을 표현하여 오예측 제거

#### 📌 1-3. 명령어 수준 병렬성(ILP)과 의존성
1. 데이터 의존성 체인 — `a = b + c; d = a + e;` 처럼 이전 결과에 의존해 병렬 실행이 막히는 상황
2. 레이턴시 vs 처리량 — 곱셈(latency 3~5사이클)과 ALU(latency 1사이클)가 스케줄에 미치는 차이
3. 루프 언롤링(Loop Unrolling) — 1 이터레이션을 N개로 확장하여 의존성 연쇄를 끊고 ILP 향상
4. 컴파일러의 명령어 스케줄링 — 컴파일러가 의존성을 분석하여 명령어 순서를 자동 재배치하는 방법
5. `perf stat` 측정 — `instructions`·`cycles`·`branches`·`branch-misses` 카운터로 ILP·예측률 측정

#### 📌 1-4. 하드웨어 보안 취약점과 소프트웨어 대응
1. Spectre 변형(Variant 1·2) — 투기 경로에서 캐시 사이드채널로 메모리 내용 유출
2. Meltdown — 커널 메모리를 사용자 공간에서 읽을 수 있는 OoOE 기반 취약점
3. 커널 패치(KPTI/Spectre v2 Retpoline) — 커널-사용자 페이지 테이블 분리, 간접 점프 대체
4. 패치 성능 비용 — 시스템 콜 경로의 2~40% 스루풋 저하와 실제 영향도 측정 방법
5. 최신 CPU의 마이크로코드 업데이트 — eIBRS·BHI-Ctrl 등 하드웨어 레벨 완화 기법 개요

- **강의 내용**: 슈퍼스칼라·OoOE·투기 실행, 분기 예측 코드 패턴, 의존성·ILP, 보안 취약점과 소프트웨어 영향
- **실습 내용**: `perf stat -e cycles,instructions,branch-misses,cache-misses ./program` 으로 정렬 전/후 배열 조건문의 branch-miss 변화 측정
- **과제**: 동일한 배열 탐색을 정렬된 경우·랜덤 케이스로 나눠 `perf stat` 결과 비교 + 분기 예측 관점 분석 리포트

---

### 2주차
- **대주제**: 어셈블리 읽기와 분석 — 컴파일러가 만든 코드를 직접 읽는다
- **세부학습목표**: x86-64 핵심 명령어와 함수 호출 규약을 이해하고, `objdump`·Godbolt로 C 코드가 어셈블리로 어떻게 변환되는지 직접 추적한다.

#### 📌 2-1. x86-64 핵심 명령어 세트
1. 정수 레지스터 — rax·rbx·rcx·rdx·rsi·rdi·rsp·rbp·r8~r15, 8/16/32/64비트 서브 레지스터
2. 데이터 이동 — `mov`·`lea`·`xchg`, 메모리 주소 표현 `[rbx + rax*4 + 8]` 읽는 방법
3. 산술·논리 — `add`·`sub`·`imul`·`and`·`or`·`xor`·`shl`·`shr`·`sar` 기본 명령어
4. 비교와 분기 — `cmp`·`test` 결과가 RFLAGS에 미치는 영향, `jz`·`jne`·`jl`·`jge` 분기 명령어
5. 함수 관련 — `call`·`ret`·`push`·`pop`, RIP 상대 주소로 데이터 참조하는 방법

#### 📌 2-2. x86-64 함수 호출 규약(System V ABI)
1. 인자 전달 레지스터 — 정수 인자: rdi·rsi·rdx·rcx·r8·r9 순서, 부동소수점: xmm0~xmm7
2. 반환값 — 정수: rax, 64비트 쌍: rdx:rax, 부동소수점: xmm0
3. 호출자 저장 vs 피호출자 저장 레지스터 — rbx·rbp·r12~r15는 함수가 반드시 복원
4. 스택 프레임 구조 — 반환 주소·이전 rbp·지역 변수 배치, 16바이트 정렬(SSE 요구 사항)
5. 적색 구역(Red Zone) — System V ABI에서 rsp 위 128바이트를 리프 함수가 마음대로 사용하는 최적화

#### 📌 2-3. Godbolt와 objdump로 어셈블리 분석
1. Godbolt(godbolt.org) — 브라우저에서 C/C++/Rust를 실시간으로 어셈블리 변환·하이라이팅
2. `-O0` 어셈블리 특징 — 모든 변수가 스택에서 읽히고 쓰임, 디버그 용이하나 느린 이유
3. `-O2` 어셈블리 특징 — 레지스터 변수 할당, 루프 변환, 인라이닝, CSE 적용
4. `objdump -d -M intel 바이너리` — 실제 컴파일된 바이너리를 Intel 문법으로 역어셈블
5. 소스 라인과 어셈블리 매핑 — `objdump -dS`(디버그 심볼 포함)로 C 코드와 어셈블리 대응 확인

#### 📌 2-4. 인라인 어셈블리와 컴파일러 힌트
1. GCC `asm` 문법 — `asm volatile("명령어" : 출력 : 입력 : 클로버)` 기본 형식
2. 실용 사례 — `CPUID` 명령어로 CPU 기능 플래그 직접 읽기, `RDTSC`로 사이클 수 측정
3. `__attribute__((noinline))` — 함수 인라이닝 방지, 프로파일링 시 함수 경계 유지
4. `__builtin_expect` — 컴파일러에 분기 확률 힌트, likely/unlikely 매크로 관용 표현
5. `volatile` vs `atomic` — 컴파일러 최적화 방지(volatile)와 메모리 순서 보장(atomic)의 차이

- **강의 내용**: x86-64 핵심 명령어, System V ABI 함수 호출 규약, Godbolt·objdump 분석, 인라인 어셈블리
- **실습 내용**: C로 작성한 버블 정렬·피보나치 함수를 Godbolt에서 `-O0`·`-O2`·`-O3`로 변환 → 어셈블리 줄 수·레지스터 사용 비교
- **과제**: C 함수 3개(루프·재귀·구조체 접근)를 `objdump -dS`로 역어셈블하여 소스 라인별 어셈블리 주석 달아 제출

---

### 3주차
- **대주제**: 컴파일러 최적화 원리 — 컴파일러가 코드를 어떻게 개선하는가
- **세부학습목표**: GCC/Clang 컴파일 파이프라인의 단계를 이해하고, LLVM IR을 읽으며 주요 최적화 패스가 코드를 어떻게 변환하는지 분석한다.

#### 📌 3-1. 컴파일 파이프라인 전체 흐름
1. 전처리(Preprocessor) — 매크로 확장·`#include` 처리, `gcc -E` 로 전처리 결과 확인
2. 파싱 + AST 생성 — 소스를 추상 구문 트리(Abstract Syntax Tree)로 변환, `clang -ast-dump`로 비교
3. IRgen — AST를 플랫폼 독립 중간 표현(LLVM IR / GCC GIMPLE)으로 변환
4. 최적화 패스(Middle-end) — IR 레벨에서 여러 최적화 패스를 순서대로 적용
5. 코드 생성(Back-end) — 최적화된 IR을 타깃 아키텍처(x86·ARM·RISC-V) 어셈블리로 변환

#### 📌 3-2. 주요 최적화 패스 이해
1. 인라이닝(Inlining) — 함수 호출 오버헤드 제거, 단 코드 크기 증가·I-캐시 미스 위험
2. CSE(Common Subexpression Elimination) — 동일 계산을 한 번만 수행하도록 재사용
3. DCE(Dead Code Elimination) — 실행되지 않는 코드·사용되지 않는 변수 제거
4. LICM(Loop-Invariant Code Motion) — 루프 안 변하지 않는 계산을 루프 밖으로 이동
5. 루프 언롤링·루프 벡터화 — `-O3`에서 자동으로 적용되는 루프 변환, `-fopt-info-vec`로 확인

#### 📌 3-3. LLVM IR 읽기
1. LLVM IR 기본 구조 — `define`, `alloca`, `load`, `store`, `br`, `ret` 핵심 명령어
2. SSA 형식(Static Single Assignment) — 변수가 단 한 번만 정의되는 특성과 최적화 용이성
3. `clang -emit-llvm -S` — C 소스에서 사람이 읽을 수 있는 `.ll` 텍스트 IR 생성
4. IR 레벨 변환 비교 — `-O0` IR과 `-O2` IR에서 같은 함수가 어떻게 달라지는지 비교
5. `opt` 도구 — 특정 최적화 패스만 선택 적용하여 패스 효과를 격리하여 분석

#### 📌 3-4. 프로파일 기반 최적화(PGO)와 링크 시간 최적화(LTO)
1. PGO(Profile-Guided Optimization) — 첫 빌드(Instrumented) → 실제 실행 → 재빌드로 핫패스 최적화
2. PGO 효과 — 분기 예측 힌트·인라이닝 대상 확대, 현업에서 1~15% 추가 성능 개선
3. LTO(Link-Time Optimization) — 링크 단계에서 전체 프로그램 IR 수준 최적화, 모듈 경계 인라이닝
4. AutoFDO — `perf` 데이터를 피드백으로 활용하는 구글의 자동화 PGO 방법
5. `__attribute__((hot))`·`__attribute__((cold))` — 함수 온도 힌트로 코드 배치·최적화 우선순위 제어

- **강의 내용**: 컴파일 파이프라인(전처리→LLVM IR→최적화→코드생성), 주요 최적화 패스, LLVM IR 읽기, PGO·LTO
- **실습 내용**: `clang -emit-llvm -S -O0`·`-O2` 로 동일 함수의 IR 비교 + `opt -passes=inline,mem2reg` 단계 적용 실험
- **과제**: 반복 호출되는 C 함수에 PGO 3단계(빌드→프로파일 실행→재빌드) 적용 후 성능 변화 측정 리포트

---

### 4주차
- **대주제**: 캐시 구조와 캐시 친화적 프로그래밍 — 메모리 접근이 성능을 결정한다
- **세부학습목표**: L1/L2/L3 캐시의 내부 구조를 이해하고, 데이터 구조와 접근 패턴을 바꿔 실제 코드의 캐시 미스를 줄이는 프로그래밍 기법을 체험한다.

#### 📌 4-1. 캐시 계층 구조와 접근 비용
1. L1·L2·L3 캐시 — 용량·지연시간·공유 범위(코어 전용 vs 공유) 비교: L1(4사이클)·L2(12)·L3(40)·DRAM(200+)
2. 캐시 라인(Cache Line) — 현대 CPU의 64바이트 단위, 인접 데이터가 함께 로드되는 이유
3. 공간적 지역성 — 연속 메모리 접근 시 캐시 라인 단위 프리페치로 미스 감소
4. 시간적 지역성 — 최근 접근한 데이터가 곧 다시 필요할 확률, 캐시 잔류 시간 극대화
5. `getconf -a | grep CACHE` / `lscpu` — 실제 시스템의 캐시 크기·연관도·라인 크기 확인

#### 📌 4-2. 캐시 친화적 데이터 구조 설계
1. 행 우선 접근(Row-Major) — C/C++의 2D 배열을 행 방향으로 순차 접근하는 것이 캐시 효율적인 이유
2. 열 우선 접근(Column-Major) — FORTRAN/MATLAB 패턴, C에서 행렬 전치 미스율 실험
3. 구조체 배열(AoS) vs 배열의 구조체(SoA) — SIMD·GPGPU에서 SoA가 유리한 이유와 변환 방법
4. 타일링(Cache Blocking) — (N×N) 행렬 곱에서 캐시 크기에 맞는 블록 단위로 쪼개는 기법
5. 패딩(Padding)과 정렬 — 구조체 멤버를 캐시 라인 경계에 맞춰 배치하여 False Sharing·라인 분할 방지

#### 📌 4-3. 캐시 미스 유형과 진단
1. Cold(Compulsory) 미스 — 처음 접근하는 데이터, 프리페치 또는 워밍업으로 부분 해소
2. Capacity 미스 — 워킹 셋이 캐시 크기를 초과, 알고리즘의 공간 복잡도 감소가 근본 해결책
3. Conflict 미스 — 다른 데이터가 동일 캐시 라인 슬롯에 충돌, 패딩·기억 배치 변경으로 해소
4. Cachegrind(`valgrind --tool=cachegrind`) — 명령어별 캐시 히트·미스 수 측정, `cg_annotate`로 소스 표시
5. `perf stat -e cache-misses,cache-references` — 실행 중 캐시 미스율 측정, 최적화 전/후 비교

#### 📌 4-4. False Sharing, 프리페치, NUMA 친화
1. False Sharing — 서로 다른 코어가 같은 캐시 라인 안의 다른 변수를 수정할 때 발생하는 불필요한 일관성 트래픽
2. False Sharing 재현 — `std::atomic<int>` 두 개를 나란히 선언 vs 64바이트 패딩 후 성능 비교
3. 소프트웨어 프리페치 — `__builtin_prefetch(addr, rw, locality)` 로 다음 루프 데이터를 미리 캐시에 올리기
4. NUMA 친화 메모리 할당 — `numactl --membind 0` / `numa_alloc_onnode()` 로 원격 메모리 접근 방지
5. Huge Page와 TLB 미스 감소 — `mmap(MAP_HUGETLB)` / `/proc/sys/vm/nr_hugepages` 설정과 대규모 데이터 처리 효과

- **강의 내용**: 캐시 계층·접근 비용, AoS vs SoA·타일링·패딩, 미스 유형·Cachegrind 진단, False Sharing·NUMA
- **실습 내용**: (1) 행/열 우선 접근 행렬 곱 캐시 미스 실험 (2) False Sharing 패딩 전/후 멀티스레드 성능 측정
- **과제**: 1024×1024 행렬 곱을 AoS·SoA·타일링(블록 크기 3가지) 버전으로 구현 후 Cachegrind 미스율 + 실행 시간 비교 리포트

---

### 5주차
- **대주제**: 메모리 모델과 원자 연산 — 동시성의 기반
- **세부학습목표**: 최적화와 메모리 재배치 현상을 파악하여 스레드가 안전하게 통신하는 원리를 학습한다.

#### 📌 5-1. 메모리 순서 문제의 실체
1. 컴파일러 재배치 — 단일 스레드 의미론을 유지하며 명령어 순서를 바꾸는 컴파일러 최적화
2. CPU 비순서 실행 — OoOE와 스토어 버퍼로 인해 쓰기가 다른 코어에 보이는 순서가 달라지는 현상
3. 스토어 버퍼(Store Buffer) — 쓰기를 즉시 캐시에 반영하지 않고 버퍼에 적재하는 하드웨어 최적화
4. Sequential Consistency — 모든 스레드가 동일한 전역 순서로 메모리 연산을 관찰하는 가장 직관적 모델
5. Relaxed Memory Model — x86(TSO)·ARM·RISC-V의 메모리 모델이 허용하는 재배치 범위 차이

#### 📌 5-2. C++11 메모리 모델 — memory_order
1. `memory_order_relaxed` — 원자성만 보장, 다른 연산과의 순서 관계 없음, 카운터 증가에 적합
2. `memory_order_acquire` — 이 연산 이후의 읽기·쓰기가 앞으로 재배치되지 않음, "자물쇠 잠금" 의미
3. `memory_order_release` — 이 연산 이전의 읽기·쓰기가 뒤로 재배치되지 않음, "자물쇠 해제" 의미
4. `memory_order_seq_cst` — 기본값, 모든 연산에 전역적 단일 순서 강제, 가장 안전하지만 가장 느림
5. Happens-Before 관계 — release 쓰기와 acquire 읽기가 같은 변수에서 만나면 SW 순서 보장 성립

#### 📌 5-3. 원자 연산과 동시성 제어 기초
1. `std::atomic<T>` — 플랫폼 원자 명령어(LOCK CMPXCHG)로 컴파일되어 데이터 경쟁을 막는 타입
2. 락(Lock)의 오버헤드 — 상호 배제(Mutex)가 스레드 대기와 컨텍스트 스위칭을 유발하는 원리
3. CAS(Compare-And-Swap) — 기대값과 일치하면 새 값으로 교체하는 락 프리 알고리즘의 기초
4. 스레드 안전성(Thread-Safety) 개념 — 여러 스레드가 동시 접근할 때 데이터 무결성을 유지하는 원칙
5. 동시성 버그 탐지 도구 소개 — ThreadSanitizer로 데드락(Deadlock)과 데이터 경쟁 찾기

#### 📌 5-4. 동시성과 메모리 안정성
1. 컴파일러 배리어 — 코드가 최적화 단계에서 무작위로 위치가 바뀌는 것을 막는 힌트
2. CPU 캐시 일관성 프로토콜(MESI) 개념 — 여러 코어가 동일 메모리를 바라볼 때 발생하는 소통 메커니즘
3. x86 구조와 ARM 구조의 차이점 기초 — 메모리 동기화를 느슨하게 두는 칩셋(ARM)의 배리어 필요성
4. 멀티스레딩에서의 False Sharing 재복습 — 병렬 처리 성능이 오히려 싱글보다 나빠지는 사례 분석
5. 고급 시스템 언어(Rust 등)가 제안하는 메모리 소유권(Ownership) 통제 원리

- **강의 내용**: 락과 언락의 한계, CAS의 원리, 스레드 안전성과 메모리 일관성 통제
- **실습 내용**: ThreadSanitizer를 활용해 C++ 프로그램 내부의 숨겨진 Data Race(데이터 경쟁 상태) 현상 찾아내기
- **과제**: 동일한 루프 카운팅 작업을 싱글 스레드, Mutex 기반, `std::atomic` 기반의 3방식으로 동작시키고 실행 성능 그래프 비교 분석

---

### 6주차
- **대주제**: SIMD 벡터 프로그래밍 — 하나의 명령어로 8개 데이터를 동시에 처리한다
- **세부학습목표**: SSE/AVX2/NEON 벡터 레지스터의 구조를 이해하고, 인트린식으로 성능 크리티컬 루프를 벡터화하며 컴파일러 자동 벡터화와 비교한다.

#### 📌 6-1. SIMD 아키텍처 개요
1. SIMD 레지스터 계보 — x86: 64비트(MMX) → 128비트(SSE) → 256비트(AVX/AVX2) → 512비트(AVX-512)
2. YMM 레지스터(256비트) — 8개 float/int32 또는 4개 double을 하나의 레지스터에 보관
3. ARM NEON — 128비트 v0~v31 레지스터, 모바일·Apple Silicon AI 가속의 기반
4. 데이터 타입 — `__m256` (8×float)·`__m256i` (정수)·`__m256d` (4×double) 인트린식 타입
5. CPU 기능 플래그 확인 — `/proc/cpuinfo` 또는 `cpuid` 명령어로 AVX2/AVX-512 지원 여부 확인

#### 📌 6-2. AVX2 인트린식 프로그래밍
1. 로드·스토어 — `_mm256_loadu_ps(ptr)` (비정렬)·`_mm256_load_ps(ptr)` (32바이트 정렬 필수)
2. 산술 연산 — `_mm256_add_ps`·`_mm256_mul_ps`·`_mm256_fmadd_ps(a,b,c)` (a×b+c 단일 명령)
3. 비교·마스크 — `_mm256_cmp_ps(a,b,_CMP_GT_OS)` 로 비교 마스크 생성, 조건부 선택
4. Shuffle·Permute — `_mm256_permute_ps`·`_mm256_shuffle_epi32` 로 레인 내 요소 재배치
5. Gather/Scatter — `_mm256_i32gather_ps(base, indices, scale)` 비연속 메모리 벡터 로드

#### 📌 6-3. 자동 벡터화와 컴파일러 힌트
1. 자동 벡터화 조건 — 루프 iteration 수 컴파일 타임 분석 가능, 포인터 앨리어스 없음, 의존성 없음
2. `restrict` 키워드 — "이 포인터가 다른 포인터와 겹치지 않음"을 컴파일러에 보장, 앨리어스 분석 통과
3. `-fopt-info-vec-optimized` / `-Rpass=loop-vectorize` — 벡터화 성공·실패 원인 로그 출력
4. `#pragma omp simd` — OpenMP SIMD 지시자로 루프 벡터화 강제, 안전성 검사 생략
5. 정렬 보장 — `alignas(32)` 선언, `__builtin_assume_aligned(ptr, 32)` 로 컴파일러 정렬 가정

#### 📌 6-4. SIMD 최적화 실제 사례
1. 점 곱(Dot Product) — 스칼라 vs AVX2 FMA 구현, horizontal add(`_mm256_hadd_ps`)로 결과 합산
2. 문자열 검색 — `_mm256_cmpeq_epi8`로 32바이트씩 비교하는 SIMD 버전 `memchr`
3. Base64 인코딩 — 비트 조작과 Gather를 활용한 SIMD Base64 변환 개요
4. 이미지 처리 — 픽셀 채널(R·G·B·A) 값에 SIMD 연산으로 밝기·대비 조정
5. 성능 측정 — `perf stat -e instructions,cycles` + `chrono`로 스칼라·자동벡터화·수동인트린식 비교

- **강의 내용**: SIMD 레지스터 계보, AVX2 인트린식, 자동 벡터화 조건, SIMD 최적화 사례
- **실습 내용**: 1백만 element float 배열 합산을 스칼라·자동벡터화·AVX2 인트린식 3가지로 구현 → 실행 시간·명령어 수 비교
- **과제**: AVX2로 4×4 float 행렬 곱 인트린식 구현 + `-fopt-info-vec`으로 컴파일러 자동벡터화 실패 이유 분석 제출

---

### 7주차
- **대주제**: 시스템 프로그래밍 기초 — 커널과 소프트웨어의 경계에서 코드를 쓴다
- **세부학습목표**: 시스템 콜의 비용을 측정하고, mmap/epoll/io_uring 등 고성능 I/O 인터페이스를 이해하며, 동적 링킹과 공유 라이브러리의 동작 원리를 파악한다.

#### 📌 7-1. 시스템 콜 비용과 최소화 전략
1. 시스템 콜 비용 — 사용자↔커널 모드 전환 + Spectre 패치 오버헤드, 대략 100~300ns
2. `strace -c ./program` — 시스템 콜 종류별 호출 수·총 시간·비율 통계 출력
3. vDSO(Virtual Dynamic Shared Object) — `gettimeofday`·`clock_gettime` 을 시스템 콜 없이 사용자 공간에서 실행하는 커널 매핑 기법
4. 배치(Batching) 전략 — write 1바이트씩 N번 vs 버퍼에 모았다가 한 번에, 시스템 콜 횟수 감소
5. io_uring(Linux 5.1+) — 제출 큐와 완료 큐를 공유 메모리에 두어 시스템 콜 없이 I/O 요청 제출

#### 📌 7-2. mmap과 고성능 파일 I/O
1. `mmap()` — 파일을 가상 주소 공간에 직접 매핑, read/write 대비 메모리 복사 제거
2. Demand Paging — mmap 후 실제 접근 시 페이지 폴트 발생, 레이지 로딩의 구현 원리
3. MAP_PRIVATE vs MAP_SHARED — CoW 사본 vs 파일에 직접 반영하는 두 가지 매핑 모드
4. Huge Page + mmap — `MAP_HUGETLB` 플래그로 2MB 페이지 매핑, 대규모 데이터 TLB 미스 감소
5. `msync`·`madvise(MADV_SEQUENTIAL)` — 데이터 동기화와 접근 패턴 힌트로 OS 프리페치 유도

#### 📌 7-3. 신호(Signal)·epoll·비동기 I/O
1. 신호(Signal) — 프로세스 간 비동기 통보, `SIGINT`·`SIGTERM`·`SIGSEGV` 처리와 signal-safe 함수 제약
2. `epoll` — 수천 개의 파일 디스크립터에서 준비된 것만 알려주는 레벨·엣지 트리거 I/O 다중화
3. epoll vs select/poll — O(1) vs O(N) 이벤트 탐색 복잡도, 이유와 실측 비교
4. 비블로킹 소켓 + epoll — Node.js 이벤트 루프가 단일 스레드로 수만 연결을 처리하는 원리
5. `io_uring` 실전 — liburing으로 파일 읽기·소켓 전송 요청을 큐에 제출하고 배치로 완료 수확

#### 📌 7-4. 동적 링킹과 공유 라이브러리
1. 정적 링킹 vs 동적 링킹 — 바이너리 크기·메모리 공유·업데이트 편의성 트레이드오프
2. ELF 구조 — `.text`·`.data`·`.bss`·`.got`·`.plt` 섹션 역할과 `readelf -a`로 확인
3. PLT/GOT(Procedure Linkage Table/Global Offset Table) — 동적 심볼을 처음 호출 시 resolve하는 lazy binding
4. `LD_PRELOAD` — 기존 라이브러리 함수를 사용자 구현으로 교체하여 동작을 가로채는 방법
5. SONAME·rpath·RUNPATH — 공유 라이브러리 버전 관리와 런타임 탐색 경로 설정

- **강의 내용**: 시스템 콜 비용·vDSO·io_uring, mmap·CoW·Huge Page, epoll 비동기 I/O, PLT/GOT 동적 링킹
- **실습 내용**: (1) `strace -c` 파일 열기 종류별 시스템 콜 횟수 비교 (2) `mmap` vs `fread` 100MB 파일 처리 속도 측정
- **과제**: epoll 기반 에코 서버 구현(비블로킹 소켓 + 이벤트 루프) + 동시 접속 100·1000개에서 처리량 측정 리포트

---

### 8주차
- **대주제**: 중간고사 (평가 주간)
- **세부학습목표**: 1~7주차 (CPU 마이크로아키텍처·어셈블리 분석·컴파일러 최적화·캐시 프로그래밍·메모리 모델·SIMD·시스템 프로그래밍) 전반기 성취도 측정
- **강의 내용**: 이론 필기(어셈블리 읽기·최적화 패스·memory_order·SIMD 타입) + 실기(`perf stat` 분석 문제·Godbolt 어셈블리 주석)
- **실습 내용**: 없음
- **과제**: 없음

---

### 9주차
- **대주제**: AI GPU 컴퓨팅 — CUDA에서 JAX, MLX까지
- **세부학습목표**: 싱글 스레드의 한계를 넘어서는 GPU 스레드 구조를 파악하고 기초 병렬화를 이해한다. 나아가 NVIDIA 생태계와 Google JAX, Apple MLX 등 AI 특화 구조를 폭넓게 체험한다.

#### 📌 9-1. GPU 병렬 데이터 처리 기초
1. CPU 코어 vs GPU 코어 — 복잡한 로직 처리(수 개) vs 단순 반복 병렬 데이터 처리(수천 개) 목적성 차이
2. 워프(Warp) 계층 모델 — 32개의 스레드가 동시에 한 타임으로 동일 명령을 실행하는 그룹 스케줄 단위
3. 워프 발산(Warp Divergence) — 반복/탐색 루프 중 `if-else` 분기에서 32개 스레드의 실행 경로가 엇갈려 속도가 저하되는 문제
4. 온칩 공유 메모리(Shared Memory) — 메인 VRAM 데이터를 GPU 내부 빠른 캐시 구역에 적재해 입출력 지연을 완전히 지우는 핵심 기법
5. CUDA 커널 맵핑 — 이중 for문(2D 이미지 프로세싱 등)이 수백 개의 블록과 스레드 그리드 모델로 할당되는 원리

#### 📌 9-2. 멀티 AI 플랫폼 생태계: CUDA, 구글 JAX, 애플 MLX
1. NVIDIA CUDA 아성 — 하드웨어 자체보다 세계를 장악한 cuDNN 등 방대한 C++ 병렬 최적화 수학 라이브러리 생태계
2. 구글 JAX의 등장 — 복잡한 내부 구성을 XLA(Accelerated Linear Algebra) 컴파일러에 맡겨 TPU와 GPU 양측 모두에서 코드 속도를 극대화하는 자동 분산 프레임워크
3. 애플 MLX 라이브러리 — 일반 모델과 달리 통짜 실리콘(SoC) 환경에서 메인보드 램(M1/M2/M3) 전체를 공유 풀(Unified Memory)로 활용하여 복사 오버헤드를 제로화한 ML 설계 철학
4. 범용 프레임워크 PyTorch 엔진 메커니즘 — 파이썬 스크립트가 내부적으로 플랫폼을 인지해 알맞은 최하층 C++/CUDA/MPS 언어로 번역되는 추상화 기법
5. 차세대 그래픽 모델 선택 — 개인 장비 구조(Windows vs Mac)나 학습/추론 목적에 따른 가속 엔드포인트 선택 가이드라인

- **강의 내용**: GPU 스레드 구조 원리, 데이터 합병 접근, CUDA와 최신 JAX/MLX 아키텍처 생태계의 패러다임 차이 통찰
- **실습 내용**: 5000x5000 배열 곱셈을 순수 Python → NumPy(C 코어) → PyTorch(GPU/MPS) 환경 순서로 비교 및 속도 격차 측정
- **과제**: 자신의 기기 기반이나 무료 Cloud환경(Colab)을 통해 JAX 혹은 MLX 환경 세팅을 경험하고, AI 구동 프레임워크의 자원 활성화 화면(`nvidia-smi` 등) 리포트

---

### 10주차
- **대주제**: 싱글 프로세싱 시스템의 한계 돌파 — 병렬 처리와 대규모 처리 클러스터링
- **세부학습목표**: 싱글 처리와 병렬 처리의 근본적 차이를 구별하고, 한 대의 컴퓨터를 넘어 대규모 데이터를 묶어 처리하는 클러스터링 기반 확장 기술을 파악한다.

#### 📌 10-1. 싱글 처리 vs 병렬 처리 (Single vs Parallel)
1. 싱글 프로세싱(순차 실행) — 한 번에 한 명령어씩 일직선으로 실행하는 구조. 흐름 추적이 쉬워 디버깅에 유리하나 처리량 병목 발생
2. 병렬 처리의 두 축 — 데이터 병렬성(동일 연산을 많은 데이터에 통째로 뿌림) vs 작업 병렬성(각기 다른 파이프라인 구역을 동시에 처리)
3. 암달의 법칙(Amdahl's Law) — "코어를 100개 넣는다고 100배 빨라지지 않는다." 반드시 싱글로 실행해야 할 코드 구간이 허들로 작용하는 원리 규명
4. 동기(Synchronous) vs 비동기(Asynchronous) 논리 — 쓰레드를 계속 낭비하지 않고 I/O 대기 시간을 다른 작업에 반납하는 Node/asyncio의 효율적 모델 비교
5. 컨텍스트 스위칭의 진실 — 무리한 병렬화 스레드 상향이 도리어 코어 교체 딜레이 비용을 키우고 성능을 무너트리는 사례

#### 📌 10-2. 대규모 분산 클러스터링 모델
1. 멀티 컴퓨터 클러스터링 원리 — 여러 대의 서버(Node) 장비를 논리적 네트워크망으로 묶어 하나의 슈퍼컴퓨터로 합성시키는 인프라 통신 체계
2. 스케일 업(Scale-up) vs 스케일 아웃(Scale-out) — 자원 최상급 서버 단일 증대에 맞선 소형 상용 서버 수백대 그룹핑 확장 아키텍처 비교
3. 방대한 처리량의 제왕: Apache Spark / Hadoop — 대용량 데이터를 아주 잘게 쪼개어 수백 노드에 뿌린(Map) 후 다시 수합(Reduce)하는 MapReduce 철학
4. 무중단 시스템(HA, 로드밸런싱) — 클러스터 내의 노드가 불시 고장 나도 멈춤 없이 즉각 작업 분담을 재수행하는 Fail-over 관리 시스템
5. Ray 프레임워크 소개 — 분산 머신러닝 및 AI 워크로드를 수많은 Python 클러스터 풀 위에서 훈련·서비스하기 위한 최신 고효율 도구

#### 📌 10-3. 로컬 코어 병렬화 및 활용
1. OpenMP를 통한 C/C++ 기초 고속화 — 코드 라인에 규칙 한 줄만 추가하여 간단한 `for` 루프를 멀티코어 전원 병렬 실행으로 돌려버리는 기법
2. `Reduction` 리스크 해소 — 각 코어가 동시 계산한 개별 합산을 최종적으로 에러 없이 안전하게 단독 합치는 처리 패턴
3. 파이썬 GIL 우회 기법 (`multiprocessing`) — 고질적 인터프리터 락을 벗어나기 위해 각자 별도 시스템 프로세스를 파생시켜 100% 코어를 독점 전개하는 활용

- **강의 내용**: 싱글·병렬 처리 개념 심층 이해, 멀티 클러스터 대용량 장비 확장의 패러다임, 암달의 법칙, Ray/Spark 분산 모델
- **실습 내용**: 1~2천만 개 숫자 패턴 검색 작업을 기초 싱글 루핑 → Python 멀티프로세싱 병렬화로 각각 구동하고 벤치마크 시간 확인
- **과제**: 로컬 멀티코어 환경에서 스레드 워커 수를 1개에서 CPU 코어 수의 3배까지 단계적으로 올려가며 속도 변화 그래프를 캡처하고 원인(암달/컨텍스트 비용) 에세이 제출

---

### 11주차
- **대주제**: 성능 분석과 최적화 워크플로우 — 병목을 찾아 증거로 개선한다
- **세부학습목표**: `perf`·Cachegrind·Valgrind·Roofline 모델을 체계적으로 활용하여 프로그램 병목을 정확히 진단하고, 측정 기반의 최적화 프로세스를 완성한다.

#### 📌 11-1. perf와 Flame Graph
1. `perf stat` — CPU 사이클·명령어·캐시 미스·분기 오예측을 하드웨어 카운터로 측정
2. `perf record -g` + `perf report` — 프로그램 실행 중 콜 스택 샘플링, 함수별 CPU 비율 출력
3. Flame Graph 생성 — `perf script | stackcollapse-perf.pl | flamegraph.pl > fg.svg`으로 시각화
4. Flame Graph 읽기 — 폭이 넓을수록 CPU 시간이 많음, 색상은 의미 없음, 플랫폼이 넓으면 병목
5. `perf trace` — 시스템 콜 추적, 타이밍·인자·반환값을 실시간으로 출력 (`strace` 저오버헤드 대안)

#### 📌 11-2. Valgrind — Cachegrind·Massif·Helgrind
1. Cachegrind — 명령어별 I1·D1·LL 캐시 히트·미스 수 측정, `cg_annotate`로 소스 라인 주석
2. Callgrind — 함수 호출 그래프와 명령어 비용 분석, KCachegrind GUI로 시각화
3. Massif — 힙 메모리 사용 이력을 시간 축으로 기록, ms_print로 최고점 분석
4. Helgrind — 스레드 간 데이터 경쟁(Race Condition) 동적 탐지, Lock-Order Violation 감지
5. AddressSanitizer(ASan) — 컴파일러 계측 방식 메모리 오류(버퍼 오버플로우·Use-After-Free) 런타임 감지

#### 📌 11-3. Roofline 모델과 병목 유형 판별
1. Roofline 모델 구성 — X축: 연산 강도(FLOP/Byte), Y축: 성능(GFLOP/s), 두 한계선 교차점이 지붕
2. 메모리 바운드 분석 — 커널이 지붕 교차점 왼쪽에 있으면 메모리 대역폭이 병목
3. 컴퓨트 바운드 분석 — 커널이 교차점 오른쪽에 있으면 피크 FLOPS가 병목
4. `likwid-perfctr` — FLOP 수·메모리 트래픽을 정확히 측정하여 Roofline 데이터 수집
5. Empirical Roofline Toolkit(ERT) — 메모리 대역폭·피크 FLOPS 실측값으로 Roofline 플롯 자동 생성

#### 📌 11-4. 벤치마킹 방법론과 측정 신뢰도
1. 마이크로벤치마크 함정 — 컴파일러 최적화로 측정 대상 코드가 제거되는 경우와 방지 방법
2. `volatile`·`__asm__ volatile("")` — 컴파일러가 벤치마크 코드를 제거하지 못하게 막는 관용구
3. Google Benchmark — C++ 벤치마크 라이브러리, 통계(mean·stddev·CV)·반복 횟수 자동 조정
4. 외부 요인 통제 — CPU frequency scaling 고정(`cpupower frequency-set`)·turbo boost 비활성화
5. 통계 유의성 — 5회 이상 측정·최솟값 vs 평균 선택 기준·95% 신뢰구간으로 결과 제시

- **강의 내용**: perf·Flame Graph, Cachegrind·Massif·Helgrind·ASan, Roofline 모델, 벤치마킹 방법론
- **실습 내용**: 자체 작성한 행렬 곱 → perf Flame Graph → Cachegrind → Roofline 순서로 병목 진단 → 최적화 적용 → 재측정
- **과제**: 실제 사용 중인 오픈소스 프로젝트(자유 선택)에서 CPU 핫 함수를 Flame Graph로 3개 찾고 최적화 가능 이유를 분석한 리포트 제출

---

### 12주차
- **대주제**: 임베디드 소프트웨어 — 자원 제약 환경에서 소프트웨어로 하드웨어를 제어한다
- **세부학습목표**: 베어메탈 C 프로그래밍의 링커 스크립트·스타트업 코드를 이해하고, FreeRTOS 태스크·큐·세마포어로 멀티태스크 펌웨어를 구현한다.

#### 📌 12-1. 베어메탈 프로그래밍과 시스템 초기화
1. 링커 스크립트(.ld) — Flash·RAM 메모리 지도 정의, `.text`·`.data`·`.bss` 섹션 배치
2. 스타트업 코드(startup.s) — 리셋 벡터에서 시작, .data 초기화·.bss 제로화 후 main() 호출
3. 인터럽트 벡터 테이블 — MCU 시작 주소에 위치한 예외 핸들러 주소 배열, `__attribute__((section(".isr_vector")))`
4. 빌드 스크립트 — `arm-none-eabi-gcc -mcpu=cortex-m4 -mthumb` + 링커 플래그 (`-Wl,-Map,output.map`)
5. OpenOCD/GDB — JTAG/SWD 디버거 연결, 브레이크포인트·레지스터 읽기·플래시 프로그래밍

#### 📌 12-2. 인터럽트 기반 프로그래밍
1. 폴링 vs 인터럽트 — While 루프로 상태 확인 vs 이벤트가 ISR을 자동 호출하는 방식 비교
2. ISR 작성 원칙 — 짧고 빠르게, 전역 플래그 설정 후 메인 루프에서 처리하는 Deferred Processing 패턴
3. NVIC(Nested Vectored Interrupt Controller) — 우선순위 설정·중첩 인터럽트·선점 처리
4. 타이머 인터럽트 — TIM 모듈의 PSC(Prescaler)·ARR(Auto-Reload), 정밀 주기 이벤트 생성
5. 인터럽트 안전 공유 변수 — `volatile` 키워드와 임계 구역(`__disable_irq`·`__enable_irq`) 처리

#### 📌 12-3. FreeRTOS 심화
1. 태스크(Task) — `xTaskCreate(func, name, stack, param, priority, handle)`, 우선순위 선점 스케줄링
2. 큐(Queue) — `xQueueSend`·`xQueueReceive`, ISR에서 안전한 `xQueueSendFromISR`
3. 뮤텍스·세마포어 — 공유 자원 보호(이진 세마포어)·자원 개수 계수(카운팅 세마포어)
4. 알림(Notification) — 가벼운 태스크 간 신호, 세마포어보다 낮은 오버헤드
5. 우선순위 역전(Priority Inversion) — 낮은 우선순위 태스크가 자원을 점유할 때 미들 태스크가 진행을 막는 문제와 우선순위 상속 해결

#### 📌 12-4. 자원 제약 환경 최적화
1. Flash 절약 — LTO·`-Os`·함수 속성 `__attribute__((section(".text.fast")))`로 코드 크기 감소
2. RAM 절약 — 정적 할당, 스택 크기 최소화, `__attribute__((noinit))` 재부팅 유지 변수
3. 전력 관리 — Sleep/Stop/Standby 모드 진입, WFI(Wait-For-Interrupt) 명령으로 유휴 전류 최소화
4. 실시간 성능 검증 — Logic Analyzer로 태스크 전환 지연 측정, `uxTaskGetStackHighWaterMark`로 스택 사용량 확인
5. UART 디버깅 vs SWO ITM — 시리얼 출력 대신 CoreSight SWO로 런타임 로그를 분리 핀으로 비침습 출력

- **강의 내용**: 링커 스크립트·스타트업 코드, 인터럽트 기반 프로그래밍, FreeRTOS 태스크·큐·세마포어, 자원 제약 최적화
- **실습 내용**: STM32에서 FreeRTOS 3 태스크(버튼ISR → 큐 → 처리 → UART 출력) 구현 + 스택 사용량 모니터링
- **과제**: UART 수신 인터럽트 → FreeRTOS 큐 → 처리 태스크 → LED 출력 전체 파이프라인 구현 + 최악 지연(Worst-Case Latency) 측정 리포트

---

### 13주차
- **대주제**: 소프트웨어의 하드웨어 추상화 — 하드웨어 가상화와 언어 런타임
- **세부학습목표**: 물리적 하드웨어를 소프트웨어로 분할·가공하여 사용하는 하드웨어 가상화 기술을 터득하고, Java/Python 등의 코드가 가상머신 위에서 실행되는 런타임 원리를 기초부터 이해한다.

#### 📌 13-1. 하드웨어 가상화(Virtualization) 인프라
1. 가상화 기술의 정의 — 거대한 한 대의 물리적 하드웨어(CPU, RAM)를 논리적으로 쪼개 여러 독립된 컴퓨터 운영체제를 동시 구동하는 기반 메커니즘
2. 하이퍼바이저(Hypervisor) 구조 — 기존 OS 창에 띄우는 Type 2(VMware Workstation/UTM) vs 하드웨어 위에 직접 입주시키는 제어 전담 Type 1(ESXi/Proxmox) 모델 차이
3. 하드웨어의 가상화 어시스트 반격(Intel VT-x / AMD-V) — 하드웨어 명령어를 직접 확장해 소프트웨어의 무거운 에뮬레이션 딜레이를 칩 레벨에서 완전히 제거해버린 혁명
4. 메모리 이중화 배분 및 I/O 가상화 — 가상 기기가 본체를 속이고 할당받은 자기 전용 램(SLAT)과 네트워크 카드 한장을 수십 조각 내어 독점 지분을 갖는 가상네트워킹(SR-IOV) 기법
5. 공용 클라우드를 이끈 가상 머신(VM) 기반 빌드 — 자유롭게 서버 파티션을 끊어 개인 사용자로 배포하는 대규모 모던 클라우드의 비즈니스 패러다임

#### 📌 13-2. 가상 머신(VM)과 컨테이너(Docker)의 세계계층 분석
1. 전통 OS 전체 가상화(VM) — 철저히 고립된 각각의 덩치 큰 게스트 커널이 묶여 가장 안정적이지만 느린 구조
2. 컨테이너 소프트 가상화(Docker) — 밑바탕 OS 커널 엔진 하나만 공유해 초고속 동작을 달성하며 실행 환경 파일만 슬림 격리(Namespace/cgroup 조작)하는 차세대 개발의 표준
3. 클러스터 관리 전담반 Kubernetes(K8s) — 수천, 수만 장의 도커 컨테이너를 통합 관리하고 자동 복구 및 스케일 아웃을 일괄 실행하는 클라우드 생태계 관제
4. WebAssembly(WASM) 타깃 격리 — 브라우저·서버 안에서 가장 빠르고 가볍게 실행되는 캡슐화 샌드박스의 또다른 가상화 규격 돌풍현상

#### 📌 13-3. 애플리케이션 레벨의 가상화 (언어 런타임 제어)
1. CPython/JVM 가상 실행 모델 — 물리 하드웨어 기계어 자리를 대체하기 위해 자기 스스로 바이트코드 가상 머신 인터프리터를 구현하여 이식성 방패를 전개하는 언어 디자인
2. JIT(Just-In-Time) 컴파일러 — 프로그램이 동작하는 "당장 그 순간" 자주 지나는 구간(핫스팟)을 실제 물리 코어용 기계어로 즉석 번역하여 속도를 급상승시키는 고능 효율화
3. 메모리 가비지 컬렉션(GC) 도입 — 개발자가 해제 코드를 치지 않아도 시스템 뒤에서 쓰레기 객체를 마킹해 제거하는 자동 소거 매커니즘
4. 추상화의 영수증(성능 패널티) — C/C++ 정밀 수동 통제의 막강한 속도 체감 vs Java/Python의 초고도 개발 생산성 사이의 타협과 가상화 계층 극복의 한계선 비교

- **강의 내용**: VT-x 하드웨어 보조 가상화 기술, 컨테이너 엔진의 가벼움 원리 증명, JIT 컴파일 및 자동 메모리 GC 로직의 이해
- **실습 내용**: 개별 기기에 Docker Engine을 올려 독립 가상 컨테이너 실행과 앱 가상배포의 차이를 시스템 자원 점유 모니터링으로 대조
- **과제**: 동일한 루프·메모리 반복 스크립트를 C 컴파일 방식과 Python 방식, JVM 방식으로 구동한 뒤 각각에 숨겨진 JIT 구동 시점의 속도 역전 현상 분석 리포트 제출

---

### 14주차
- **대주제**: 기말고사 및 종합 발표 — 하드웨어 위에서 달리는 소프트웨어를 설계한다
- **세부학습목표**: 한 학기 학습(CPU 마이크로아키텍처·어셈블리·컴파일러·캐시·메모리 모델·SIMD·GPU·성능 분석·임베디드·JIT)을 통합하여 실제 성능 개선 시나리오를 발표한다.

#### 📌 14-1. 전체 범위 복습
1. 코드 실행 경로 전체 — 소스 코드 → 컴파일(IR·최적화) → 바이너리 → CPU 파이프라인 → 캐시 → DRAM
2. 성능 진단 결정 트리 — CPU-bound(perf·Flame Graph) / Memory-bound(Cachegrind·Roofline) / I/O-bound(strace·iostat) 분류
3. 병렬화 선택 기준 — 데이터 병렬(SIMD·OpenMP) / 태스크 병렬(스레드·multiprocessing) / GPU(CUDA) 선택 근거
4. 메모리 모델 체크리스트 — 공유 변수 존재 시 acquire·release 선택, False Sharing 검사, NUMA 친화 할당
5. JIT·GC 영향 파악 — 응용 레이어 언어(Python·JVM)에서 하드웨어 성능을 저해하는 런타임 요인

#### 📌 14-2. 팀 발표 — 성능 개선 보고서
1. **성능 최적화 트랙** — 실제 오픈소스 코드베이스에서 병목을 발견하고 perf·Cachegrind로 원인 분석 후 패치 적용 전/후 비교
2. **CUDA 가속 트랙** — CPU 기준 코드(NumPy/SciPy)를 커스텀 CUDA 커널로 대체하고 Nsight로 최적화 효과 측정
3. **임베디드 시스템 트랙** — FreeRTOS 기반 IoT 장치에서 최악 지연을 측정하고 태스크 설계 개선으로 줄이는 과정 기록
4. **컴파일러 최적화 트랙** — C/C++ 핵심 함수에 PGO·LTO·SIMD 인트린식을 적용하여 -O2 기준 대비 성능 향상 문서화
5. 동료 피어 평가 + 교수 라이브 질의응답

#### 📌 14-3. 다음 단계 — 심화 학습 로드맵
1. 운영체제(OS) 연계 — 이 학기에서 배운 캐시 일관성·페이지 테이블·시스템 콜이 OS에서 어떻게 관리되는지 연결
2. 컴파일러 심화 — LLVM 백엔드 패스 작성, MLIR로 AI 연산 컴파일러 개발 입문
3. 고성능 컴퓨팅(HPC) 커리어 — Top500·SC 컨퍼런스, CUDA 자격증(NCA), OpenMP/MPI 상용 교육
4. ML 시스템 엔지니어링 — PyTorch Inductor·XLA·TVM 컴파일러 스택, 커스텀 CUDA Extension 개발
5. "내가 이 학기에 가장 놀랐던 하드웨어-소프트웨어 연결 순간" 개인 회고 2페이지

- **강의 내용**: 전체 범위 복습, 팀별 성능 개선 발표
- **실습 내용**: 팀 발표 + 라이브 데모 (perf Flame Graph·CUDA 커널·FreeRTOS 측정 결과 실시간 시연)
- **과제**: 최종 발표 슬라이드 + GitHub 소스 코드 + 개인 학습 회고 리포트 제출

---

## 5. 평가 방식

| 항목 | 비율 | 설명 |
|------|------|------|
| 출석 | 10% | 성실한 참여 |
| 주간 실습 보고서 | 25% | 매주 perf·Godbolt·CUDA·Valgrind 실습 캡처 + 분석 리포트 |
| 중간 개인 프로젝트 | 15% | 8주차까지 배운 캐시·어셈블리·컴파일러 최적화를 특정 코드에 적용한 개인 최적화 리포트 |
| 중간고사 | 20% | 1~7주차 이론·코드 읽기 필기 |
| 기말 팀 발표 | 30% | 성능 개선 문서화·구현 완성도·측정 신뢰도·발표 명확성 |

---

## 6. 선수 지식 확인 목록

수강 전 다음 내용을 스스로 설명할 수 있어야 합니다:

- [ ] C/C++로 포인터·배열·구조체를 능숙하게 다룰 수 있다
- [ ] CPU의 Fetch–Decode–Execute 사이클을 설명할 수 있다
- [ ] L1/L2/L3 캐시가 무엇이고 왜 존재하는지 설명할 수 있다
- [ ] 멀티스레드 프로그램에서 경쟁 조건이 발생하는 예를 들 수 있다
- [ ] 가상 메모리와 페이지 테이블의 역할을 개념 수준에서 설명할 수 있다
- [ ] GPU가 CPU보다 행렬 곱에 유리한 이유를 병렬성 관점에서 설명할 수 있다

---

## 7. 교수자 유의사항

- **"측정 먼저, 최적화 나중" 원칙 반복 강조**: 추측 없이 perf·Cachegrind 데이터를 보고 최적화하는 습관을 1주차부터 훈련하세요. "느릴 것 같아서 최적화했다"는 보고서는 반드시 피드백하세요.
- **회로 설계 내용 배제**: 이 과목은 EE가 아닙니다. Verilog·FPGA·K-Map은 언급만 해도 과목 방향이 흔들립니다. 관련 질문은 명확히 "EE 과목 범위"임을 안내하세요.
- **Linux 실습 환경 사전 준비**: `perf`·`valgrind`·`CUDA Toolkit`은 macOS에서 완전히 동작하지 않습니다. WSL2·Docker 또는 공용 Linux 서버를 사전에 준비하세요.
- **CUDA는 Colab 활용 가능**: GPU가 없는 학생은 Google Colab A100/T4 환경을 활용합니다. Nsight Compute는 로컬 환경이 필요하므로 대안(nvcc --ptxas-options)을 안내하세요.
- **언어는 C/C++ + Python 혼합**: 어셈블리·캐시·SIMD는 C/C++, 벤치마킹·asyncio는 Python, GPU는 CUDA C++ + Python으로 진행합니다.
"""

with open('/Users/hojin/dev/jinysite/basic/computer2.md', 'w', encoding='utf-8') as f:
    f.write(content)

print("Repair completed!")
